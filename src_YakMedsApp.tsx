// Put your entire code (the one you posted above) here, but
// change all imports from "/components/ui/..." to "./components/ui/..." (relative path)
import { useState, useRef, useEffect } from 'react'
import { Button } from "./components/ui/button"
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "./components/ui/card"
import { Input } from "./components/ui/input"
import { Label } from "./components/ui/label"
import { Avatar, AvatarFallback, AvatarImage } from "./components/ui/avatar"
import { Badge } from "./components/ui/badge"
import { Mountain, Upload, Camera, User, Bell, MapPin, Phone, Edit, MessageCircle, FileText, CheckCircle, Package, Truck, LogOut, ChevronRight, X, Image, Clock, AlertCircle, ArrowLeft } from 'lucide-react'
// ... rest of your posted code ...